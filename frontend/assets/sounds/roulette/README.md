# ルーレット用サウンド

割り勘ルーレットで使用する単一音源をこのフォルダに配置します。

使用ファイル:

- `rouletteSound.wav` — 回転開始から結果決定までのルーレット音

Flutterからは、例えば次のアセットパスで読み込みます。

```dart
AssetSource('sounds/roulette/rouletteSound.wav')
```

ルーレット開始時に1回再生し、回転アニメーション終了時に停止します。
利用許諾を確認できるよう、外部音源を使用する場合はライセンス情報もこのフォルダに保存してください。
