name: megapay_app
description: メガバンク発・手軽に送金できるマルチ通貨対応アプリ（MVP / Flutter Web）
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.35.0" # DropdownButtonFormField.initialValue 等の新 API を使用

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  http: ^1.2.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
